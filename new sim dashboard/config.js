/* ============================================================
   APP CONFIGURATION
   ------------------------------------------------------------
   Paste your Supabase credentials below ONE TIME.
   After this, the dashboard loads data straight away and the
   "API Settings" popup will never appear on startup again.
   ============================================================ */

window.APP_CONFIG = {
  // 👇 Paste your Supabase anon (public) key here, between the quotes
  SUPABASE_ANON_KEY: "",

  // 👇 Your Supabase RPC endpoint
  SUPABASE_API_URL: "https://picpwddoywtufoxoiyzq.supabase.co/rest/v1/rpc/get_sale_dashboard",

  // Default report date shown when the dashboard opens (YYYY-MM-DD)
  DEFAULT_REPORT_DATE: "2026-05-31"
};
